# Patterns de gestion d'état

### Vue d'ensemble des solutions de gestion d'état en React

En React, il existe plusieurs façons de gérer l'état selon la complexité et les besoins de votre application.

---

## 1. État local avec useState

### ✅ Quand utiliser
- État spécifique à un composant
- Données simples (string, number, boolean, array simple)
- Pas de partage entre composants

### Exemple
```tsx
function LoginForm() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [isSubmitting, setIsSubmitting] = useState(false);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setIsSubmitting(true);
    // logique de soumission...
    setIsSubmitting(false);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input 
        value={email} 
        onChange={(e) => setEmail(e.target.value)} 
        placeholder="Email" 
      />
      <input 
        value={password} 
        onChange={(e) => setPassword(e.target.value)} 
        placeholder="Mot de passe"
        type="password" 
      />
      <button type="submit" disabled={isSubmitting}>
        {isSubmitting ? 'Connexion...' : 'Se connecter'}
      </button>
    </form>
  );
}
```

---

## 2. État complexe avec useReducer

### ✅ Quand utiliser
- État avec plusieurs propriétés liées
- Logique de mise à jour complexe
- Plusieurs actions possibles

### Exemple
```tsx
interface FormState {
  fields: {
    email: string;
    password: string;
    confirmPassword: string;
  };
  errors: {
    email?: string;
    password?: string;
    confirmPassword?: string;
  };
  isSubmitting: boolean;
  isValid: boolean;
}

type FormAction = 
  | { type: 'SET_FIELD'; field: keyof FormState['fields']; value: string }
  | { type: 'SET_ERROR'; field: keyof FormState['errors']; error: string }
  | { type: 'CLEAR_ERRORS' }
  | { type: 'SET_SUBMITTING'; isSubmitting: boolean }
  | { type: 'RESET_FORM' };

function formReducer(state: FormState, action: FormAction): FormState {
  switch (action.type) {
    case 'SET_FIELD':
      return {
        ...state,
        fields: { ...state.fields, [action.field]: action.value },
        errors: { ...state.errors, [action.field]: undefined }
      };
    
    case 'SET_ERROR':
      return {
        ...state,
        errors: { ...state.errors, [action.field]: action.error },
        isValid: false
      };
    
    case 'CLEAR_ERRORS':
      return { ...state, errors: {}, isValid: true };
    
    case 'SET_SUBMITTING':
      return { ...state, isSubmitting: action.isSubmitting };
    
    case 'RESET_FORM':
      return initialFormState;
    
    default:
      return state;
  }
}
```

---

## 3. État partagé avec Context API

### ✅ Quand utiliser
- Données utilisées par plusieurs composants
- Éviter le props drilling
- État global simple à modéré

### Pattern : Context + useReducer

```tsx
// AuthContext.tsx
interface User {
  id: number;
  email: string;
  name: string;
}

interface AuthState {
  user: User | null;
  isLoading: boolean;
  error: string | null;
}

type AuthAction = 
  | { type: 'LOGIN_START' }
  | { type: 'LOGIN_SUCCESS'; user: User }
  | { type: 'LOGIN_ERROR'; error: string }
  | { type: 'LOGOUT' };

function authReducer(state: AuthState, action: AuthAction): AuthState {
  switch (action.type) {
    case 'LOGIN_START':
      return { ...state, isLoading: true, error: null };
    
    case 'LOGIN_SUCCESS':
      return { user: action.user, isLoading: false, error: null };
    
    case 'LOGIN_ERROR':
      return { user: null, isLoading: false, error: action.error };
    
    case 'LOGOUT':
      return { user: null, isLoading: false, error: null };
    
    default:
      return state;
  }
}

const AuthContext = createContext<{
  state: AuthState;
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
} | undefined>(undefined);

function AuthProvider({ children }: { children: React.ReactNode }) {
  const [state, dispatch] = useReducer(authReducer, {
    user: null,
    isLoading: false,
    error: null
  });

  const login = async (email: string, password: string) => {
    dispatch({ type: 'LOGIN_START' });
    try {
      const user = await authService.login(email, password);
      dispatch({ type: 'LOGIN_SUCCESS', user });
    } catch (error) {
      dispatch({ type: 'LOGIN_ERROR', error: error.message });
    }
  };

  const logout = () => {
    authService.logout();
    dispatch({ type: 'LOGOUT' });
  };

  return (
    <AuthContext.Provider value={{ state, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
}

function useAuth() {
  const context = useContext(AuthContext);
  if (!context) {
    throw new Error('useAuth must be used within AuthProvider');
  }
  return context;
}
```

---

## 4. Lifting State Up

### ✅ Quand utiliser
- Deux composants frères ont besoin de partager un état
- Communication parent-enfant
- Alternative simple au Context pour des cas limités

### Exemple
```tsx
// Parent component qui remonte l'état
function ParentComponent() {
  const [selectedProduct, setSelectedProduct] = useState<Product | null>(null);
  const [cartItems, setCartItems] = useState<CartItem[]>([]);

  const addToCart = (product: Product) => {
    setCartItems(prev => {
      const existingItem = prev.find(item => item.id === product.id);
      if (existingItem) {
        return prev.map(item =>
          item.id === product.id 
            ? { ...item, quantity: item.quantity + 1 }
            : item
        );
      }
      return [...prev, { ...product, quantity: 1 }];
    });
  };

  return (
    <div>
      <ProductList 
        onSelectProduct={setSelectedProduct}
        onAddToCart={addToCart}
      />
      <ProductDetails 
        product={selectedProduct}
        onAddToCart={addToCart}
      />
      <Cart items={cartItems} />
    </div>
  );
}
```

---

## 5. État serveur vs État client

### État serveur (React Query / SWR)
Pour les données provenant d'une API :

```tsx
import { useQuery, useMutation, useQueryClient } from 'react-query';

function ProductList() {
  const queryClient = useQueryClient();

  // Récupérer les données
  const { data: products, isLoading, error } = useQuery(
    'products',
    () => fetch('/api/products').then(res => res.json())
  );

  // Mutation pour créer un produit
  const createProductMutation = useMutation(
    (newProduct: Product) => fetch('/api/products', {
      method: 'POST',
      body: JSON.stringify(newProduct)
    }),
    {
      onSuccess: () => {
        // Invalider et re-fetcher
        queryClient.invalidateQueries('products');
      }
    }
  );

  if (isLoading) return <div>Chargement...</div>;
  if (error) return <div>Erreur: {error.message}</div>;

  return (
    <div>
      {products?.map(product => (
        <div key={product.id}>{product.name}</div>
      ))}
    </div>
  );
}
```

### État client
Pour l'état de l'interface utilisateur :

```tsx
function App() {
  // État UI local
  const [theme, setTheme] = useState<'light' | 'dark'>('light');
  const [sidebarOpen, setSidebarOpen] = useState(false);
  
  return (
    <div className={`app theme-${theme}`}>
      <Sidebar isOpen={sidebarOpen} onClose={() => setSidebarOpen(false)} />
      <ProductList />
    </div>
  );
}
```

---

## 6. Patterns avancés

### Compound State Pattern
```tsx
interface AppState {
  ui: {
    theme: 'light' | 'dark';
    sidebarOpen: boolean;
    notifications: Notification[];
  };
  user: {
    currentUser: User | null;
    preferences: UserPreferences;
  };
  data: {
    products: Product[];
    cart: CartItem[];
  };
}

// Séparer en plusieurs contextes pour éviter les re-renders inutiles
function App() {
  return (
    <UIProvider>
      <AuthProvider>
        <DataProvider>
          <Router>
            <Routes>
              {/* routes */}
            </Routes>
          </Router>
        </DataProvider>
      </AuthProvider>
    </UIProvider>
  );
}
```

### Custom Hook Pattern
```tsx
// Encapsuler la logique complexe dans des hooks personnalisés
function useShoppingCart() {
  const [items, setItems] = useState<CartItem[]>([]);

  const addItem = useCallback((product: Product) => {
    setItems(prev => {
      const existing = prev.find(item => item.id === product.id);
      if (existing) {
        return prev.map(item =>
          item.id === product.id
            ? { ...item, quantity: item.quantity + 1 }
            : item
        );
      }
      return [...prev, { ...product, quantity: 1 }];
    });
  }, []);

  const removeItem = useCallback((productId: number) => {
    setItems(prev => prev.filter(item => item.id !== productId));
  }, []);

  const total = useMemo(() => 
    items.reduce((sum, item) => sum + item.price * item.quantity, 0)
  , [items]);

  return { items, addItem, removeItem, total };
}
```

---

## Guide de décision

### 🔸 État local (useState)
- ✅ Formulaires simples
- ✅ Toggle UI (modals, dropdowns)
- ✅ État temporaire

### 🔸 État complexe (useReducer)
- ✅ Formulaires complexes avec validation
- ✅ État avec plusieurs propriétés liées
- ✅ Logique de mise à jour complexe

### 🔸 État partagé (Context)
- ✅ Thème, langue
- ✅ Utilisateur connecté
- ✅ Configuration globale

### 🔸 État serveur (React Query)
- ✅ Données API
- ✅ Cache, synchronisation
- ✅ Loading, error states

### 🔸 Bibliothèques externes (Redux, Zustand)
- ✅ Applications très complexes
- ✅ Besoins de debugging avancés
- ✅ État partagé entre plusieurs parties de l'app

---

## Exemple complet : E-commerce

```tsx
// Structure recommandée pour une app e-commerce
function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <ThemeProvider>        {/* UI global */}
        <AuthProvider>       {/* Utilisateur */}
          <CartProvider>     {/* Panier (état client) */}
            <Router>
              <Layout>
                <Routes>
                  <Route path="/" element={<HomePage />} />
                  <Route path="/products" element={<ProductsPage />} />
                  <Route path="/cart" element={<CartPage />} />
                </Routes>
              </Layout>
            </Router>
          </CartProvider>
        </AuthProvider>
      </ThemeProvider>
    </QueryClientProvider>
  );
}

// Les composants utilisent React Query pour les données serveur
function ProductsPage() {
  const { data: products } = useQuery('products', fetchProducts);
  const { addItem } = useCart(); // Context pour le panier
  const { user } = useAuth();    // Context pour l'auth
  
  return (
    <div>
      {products?.map(product => (
        <ProductCard 
          key={product.id} 
          product={product} 
          onAddToCart={addItem}
        />
      ))}
    </div>
  );
}
```