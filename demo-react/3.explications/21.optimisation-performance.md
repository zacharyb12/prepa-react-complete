# Optimisation des performances

### Vue d'ensemble
L'optimisation des performances React implique de réduire les re-renders inutiles, d'optimiser les calculs coûteux, et d'améliorer l'expérience utilisateur générale.

---

## Techniques de mesure des performances

### React DevTools Profiler

```tsx
// Activer le profiling en mode développement
function App() {
  return (
    <React.Profiler id="App" onRender={onRenderCallback}>
      <Router>
        <Routes>
          <Route path="/" element={<HomePage />} />
        </Routes>
      </Router>
    </React.Profiler>
  );
}

function onRenderCallback(
  id: string,
  phase: 'mount' | 'update',
  actualDuration: number,
  baseDuration: number,
  startTime: number,
  commitTime: number
) {
  console.log('Profiler:', {
    id,
    phase,
    actualDuration, // Temps de rendu réel
    baseDuration,   // Temps estimé sans optimisations
    startTime,
    commitTime
  });
}
```

### Performance API

```tsx
// Mesurer des opérations spécifiques
function ExpensiveComponent() {
  useEffect(() => {
    performance.mark('expensive-operation-start');
    
    // Opération coûteuse
    const result = heavyCalculation();
    
    performance.mark('expensive-operation-end');
    performance.measure(
      'expensive-operation',
      'expensive-operation-start',
      'expensive-operation-end'
    );
    
    const measurements = performance.getEntriesByName('expensive-operation');
    console.log('Durée:', measurements[0].duration, 'ms');
  }, []);

  return <div>Composant avec opération coûteuse</div>;
}
```

---

## Optimisation des listes longues

### Virtualisation avec react-window

```bash
npm install react-window react-window-infinite-loader
```

```tsx
import { FixedSizeList as List } from 'react-window';

interface Item {
  id: number;
  name: string;
  description: string;
}

function VirtualizedList({ items }: { items: Item[] }) {
  // Renderer pour chaque élément
  const Row = ({ index, style }: { index: number; style: React.CSSProperties }) => (
    <div style={style} className="list-item">
      <h3>{items[index].name}</h3>
      <p>{items[index].description}</p>
    </div>
  );

  return (
    <List
      height={600}        // Hauteur du conteneur
      itemCount={items.length}
      itemSize={120}      // Hauteur de chaque élément
      width="100%"
    >
      {Row}
    </List>
  );
}

// Pour des éléments de taille variable
import { VariableSizeList as VariableList } from 'react-window';

function VariableSizedList({ items }: { items: Item[] }) {
  const getItemSize = (index: number) => {
    // Calculer la hauteur basée sur le contenu
    return items[index].description.length > 100 ? 150 : 100;
  };

  const Row = ({ index, style }: { index: number; style: React.CSSProperties }) => (
    <div style={style} className="variable-item">
      <h3>{items[index].name}</h3>
      <p>{items[index].description}</p>
    </div>
  );

  return (
    <VariableList
      height={600}
      itemCount={items.length}
      itemSize={getItemSize}
      width="100%"
    >
      {Row}
    </VariableList>
  );
}
```

### Pagination intelligente

```tsx
function PaginatedList<T>({ 
  items, 
  itemsPerPage = 20,
  renderItem 
}: {
  items: T[];
  itemsPerPage?: number;
  renderItem: (item: T, index: number) => React.ReactNode;
}) {
  const [currentPage, setCurrentPage] = useState(1);
  
  const totalPages = Math.ceil(items.length / itemsPerPage);
  
  const currentItems = useMemo(() => {
    const startIndex = (currentPage - 1) * itemsPerPage;
    return items.slice(startIndex, startIndex + itemsPerPage);
  }, [items, currentPage, itemsPerPage]);

  const goToPage = useCallback((page: number) => {
    setCurrentPage(Math.max(1, Math.min(page, totalPages)));
  }, [totalPages]);

  return (
    <div>
      <div className="items-container">
        {currentItems.map((item, index) => 
          renderItem(item, (currentPage - 1) * itemsPerPage + index)
        )}
      </div>
      
      <div className="pagination">
        <button 
          onClick={() => goToPage(currentPage - 1)}
          disabled={currentPage === 1}
        >
          Précédent
        </button>
        
        <span>Page {currentPage} sur {totalPages}</span>
        
        <button 
          onClick={() => goToPage(currentPage + 1)}
          disabled={currentPage === totalPages}
        >
          Suivant
        </button>
      </div>
    </div>
  );
}
```

---

## Optimisation des images

### Lazy loading d'images

```tsx
function LazyImage({ 
  src, 
  alt, 
  placeholder, 
  className 
}: {
  src: string;
  alt: string;
  placeholder?: string;
  className?: string;
}) {
  const [isLoaded, setIsLoaded] = useState(false);
  const [isInView, setIsInView] = useState(false);
  const imgRef = useRef<HTMLImageElement>(null);

  // Intersection Observer pour détecter quand l'image entre dans la vue
  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setIsInView(true);
          observer.disconnect();
        }
      },
      { threshold: 0.1 }
    );

    if (imgRef.current) {
      observer.observe(imgRef.current);
    }

    return () => observer.disconnect();
  }, []);

  return (
    <div className={`lazy-image-container ${className}`} ref={imgRef}>
      {!isInView && (
        <div className="image-placeholder">
          {placeholder || <div className="skeleton-image" />}
        </div>
      )}
      
      {isInView && (
        <img
          src={src}
          alt={alt}
          onLoad={() => setIsLoaded(true)}
          style={{
            opacity: isLoaded ? 1 : 0,
            transition: 'opacity 0.3s ease'
          }}
        />
      )}
    </div>
  );
}
```

### Responsive images

```tsx
function ResponsiveImage({ 
  src, 
  alt, 
  sizes = "100vw" 
}: {
  src: string;
  alt: string;
  sizes?: string;
}) {
  const srcSet = useMemo(() => {
    const baseSrc = src.replace(/\.[^/.]+$/, ""); // Remove extension
    const extension = src.match(/\.[^/.]+$/)?.[0] || '.jpg';
    
    return [
      `${baseSrc}-400w${extension} 400w`,
      `${baseSrc}-800w${extension} 800w`,
      `${baseSrc}-1200w${extension} 1200w`,
      `${baseSrc}-1600w${extension} 1600w`,
    ].join(', ');
  }, [src]);

  return (
    <img
      src={src}
      srcSet={srcSet}
      sizes={sizes}
      alt={alt}
      loading="lazy"
    />
  );
}
```

---

## Optimisation des re-renders

### Analyse des re-renders

```tsx
// Hook pour débugger les re-renders
function useWhyDidYouUpdate(name: string, props: Record<string, any>) {
  const previous = useRef<Record<string, any>>();
  
  useEffect(() => {
    if (previous.current) {
      const allKeys = Object.keys({ ...previous.current, ...props });
      const changedProps: Record<string, { from: any; to: any }> = {};
      
      allKeys.forEach(key => {
        if (previous.current![key] !== props[key]) {
          changedProps[key] = {
            from: previous.current![key],
            to: props[key]
          };
        }
      });
      
      if (Object.keys(changedProps).length) {
        console.log('[why-did-you-update]', name, changedProps);
      }
    }
    
    previous.current = props;
  });
}

// Utilisation
function MyComponent(props: { user: User; settings: Settings }) {
  useWhyDidYouUpdate('MyComponent', props);
  
  return <div>...</div>;
}
```

### Optimisation des contextes

```tsx
// ❌ Un seul contexte pour tout
const AppContext = createContext<{
  user: User;
  theme: Theme;
  notifications: Notification[];
  updateUser: (user: User) => void;
  updateTheme: (theme: Theme) => void;
  addNotification: (notification: Notification) => void;
}>();

// ✅ Séparer en contextes spécialisés
const UserContext = createContext<{
  user: User;
  updateUser: (user: User) => void;
}>();

const ThemeContext = createContext<{
  theme: Theme;
  updateTheme: (theme: Theme) => void;
}>();

const NotificationsContext = createContext<{
  notifications: Notification[];
  addNotification: (notification: Notification) => void;
}>();

// Provider optimisé
function OptimizedProvider({ children }: { children: React.ReactNode }) {
  const [user, setUser] = useState<User | null>(null);
  const [theme, setTheme] = useState<Theme>('light');
  const [notifications, setNotifications] = useState<Notification[]>([]);

  // Mémoriser les valeurs pour éviter les re-renders
  const userValue = useMemo(() => ({
    user,
    updateUser: setUser
  }), [user]);

  const themeValue = useMemo(() => ({
    theme,
    updateTheme: setTheme
  }), [theme]);

  const notificationsValue = useMemo(() => ({
    notifications,
    addNotification: (notification: Notification) => 
      setNotifications(prev => [...prev, notification])
  }), [notifications]);

  return (
    <UserContext.Provider value={userValue}>
      <ThemeContext.Provider value={themeValue}>
        <NotificationsContext.Provider value={notificationsValue}>
          {children}
        </NotificationsContext.Provider>
      </ThemeContext.Provider>
    </UserContext.Provider>
  );
}
```

---

## Optimisation du bundle

### Tree shaking efficace

```tsx
// ❌ Import entier de la librairie
import * as _ from 'lodash';
import { Button, Input, Modal } from 'antd';

// ✅ Imports spécifiques
import debounce from 'lodash/debounce';
import throttle from 'lodash/throttle';
import Button from 'antd/es/button';
import Input from 'antd/es/input';
import Modal from 'antd/es/modal';

// ✅ Ou utiliser babel-plugin-import
// Import automatiquement optimisé
import { Button, Input, Modal } from 'antd';
```

### Code splitting stratégique

```tsx
// Grouper les composants par fonctionnalité
const AdminPanel = lazy(() => 
  import('./admin').then(module => ({ default: module.AdminPanel }))
);

const UserManagement = lazy(() => 
  import('./admin').then(module => ({ default: module.UserManagement }))
);

// Précharger les routes critiques
function HomePage() {
  useEffect(() => {
    // Précharger après l'interaction initiale
    const timer = setTimeout(() => {
      import('./ProductsPage');
      import('./CartPage');
    }, 1000);

    return () => clearTimeout(timer);
  }, []);

  return <div>...</div>;
}
```

---

## Optimisation de l'état

### Normalisation des données

```tsx
// ❌ Structure imbriquée
interface AppState {
  users: {
    id: number;
    name: string;
    posts: {
      id: number;
      title: string;
      comments: {
        id: number;
        text: string;
        author: User;
      }[];
    }[];
  }[];
}

// ✅ Structure normalisée
interface NormalizedState {
  users: {
    byId: Record<number, User>;
    allIds: number[];
  };
  posts: {
    byId: Record<number, Post>;
    allIds: number[];
  };
  comments: {
    byId: Record<number, Comment>;
    allIds: number[];
  };
}

// Helper pour normaliser
function normalizeUsers(users: User[]): NormalizedState['users'] {
  return {
    byId: users.reduce((acc, user) => {
      acc[user.id] = user;
      return acc;
    }, {} as Record<number, User>),
    allIds: users.map(user => user.id)
  };
}
```

### Selectors mémorisés

```tsx
// Utiliser des selectors pour dériver l'état
function useUserPosts(userId: number) {
  const posts = useAppSelector(state => state.posts.byId);
  
  return useMemo(() => 
    Object.values(posts).filter(post => post.authorId === userId),
    [posts, userId]
  );
}

function useActiveUsers() {
  const users = useAppSelector(state => state.users.byId);
  
  return useMemo(() => 
    Object.values(users).filter(user => user.isActive),
    [users]
  );
}
```

---

## Performance des formulaires

### Debouncing et throttling

```tsx
function SearchInput({ onSearch }: { onSearch: (term: string) => void }) {
  const [searchTerm, setSearchTerm] = useState('');
  
  // Debounce la recherche
  const debouncedSearch = useMemo(
    () => debounce((term: string) => onSearch(term), 300),
    [onSearch]
  );

  useEffect(() => {
    debouncedSearch(searchTerm);
    
    // Cleanup
    return () => {
      debouncedSearch.cancel();
    };
  }, [searchTerm, debouncedSearch]);

  return (
    <input
      value={searchTerm}
      onChange={(e) => setSearchTerm(e.target.value)}
      placeholder="Rechercher..."
    />
  );
}

// Hook personnalisé pour debounce
function useDebounce<T>(value: T, delay: number): T {
  const [debouncedValue, setDebouncedValue] = useState<T>(value);

  useEffect(() => {
    const handler = setTimeout(() => {
      setDebouncedValue(value);
    }, delay);

    return () => {
      clearTimeout(handler);
    };
  }, [value, delay]);

  return debouncedValue;
}
```

### Formulaires optimisés

```tsx
// Éviter les re-renders de tout le formulaire
function OptimizedForm() {
  const [formData, setFormData] = useState({
    email: '',
    name: '',
    message: ''
  });

  // Callback mémorisé pour chaque champ
  const updateField = useCallback((field: keyof typeof formData) => 
    (value: string) => {
      setFormData(prev => ({ ...prev, [field]: value }));
    }, []
  );

  return (
    <form>
      <FormField
        label="Email"
        value={formData.email}
        onChange={updateField('email')}
      />
      <FormField
        label="Nom"
        value={formData.name}
        onChange={updateField('name')}
      />
      <FormField
        label="Message"
        value={formData.message}
        onChange={updateField('message')}
      />
    </form>
  );
}

// Composant de champ mémorisé
const FormField = React.memo(function FormField({
  label,
  value,
  onChange
}: {
  label: string;
  value: string;
  onChange: (value: string) => void;
}) {
  return (
    <div>
      <label>{label}</label>
      <input
        value={value}
        onChange={(e) => onChange(e.target.value)}
      />
    </div>
  );
});
```

---

## Checklist d'optimisation

### ✅ Mesures préliminaires
- [ ] Profiler avec React DevTools
- [ ] Analyser le bundle avec webpack-bundle-analyzer
- [ ] Mesurer les Core Web Vitals

### ✅ Optimisations de base
- [ ] Mémoriser les composants coûteux avec React.memo
- [ ] Utiliser useCallback pour les fonctions
- [ ] Utiliser useMemo pour les calculs
- [ ] Implémenter le lazy loading des routes

### ✅ Optimisations avancées
- [ ] Virtualiser les longues listes
- [ ] Optimiser les images (lazy loading, responsive)
- [ ] Diviser les contextes
- [ ] Normaliser les structures de données complexes

### ✅ Bundle et réseau
- [ ] Code splitting efficace
- [ ] Tree shaking des librairies
- [ ] Compression et cache appropriés
- [ ] Préchargement intelligent

### ✅ Surveillance continue
- [ ] Monitoring des performances en production
- [ ] Tests de performance automatisés
- [ ] Suivi des métriques utilisateur